package uz.nagato.touragency.media.entity;

/** What a media file is attached to. GENERAL means it lives in the shared library only. */
public enum OwnerType {
    GENERAL,
    TOUR,
    DESTINATION,
    CATEGORY,
    PAGE
}
